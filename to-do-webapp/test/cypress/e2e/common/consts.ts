import { TodoObj } from "../utils/types";

export const todoName = 'Working';
export const newtodoName = 'new_todo';

export const updateTodoInfo: TodoObj = {
    name: 'updated to do',
    isComplete: true
}